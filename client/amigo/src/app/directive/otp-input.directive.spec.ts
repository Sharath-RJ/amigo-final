import { OtpInputDirective } from './otp-input.directive';

describe('OtpInputDirective', () => {
  it('should create an instance', () => {
    const directive = new OtpInputDirective();
    expect(directive).toBeTruthy();
  });
});
